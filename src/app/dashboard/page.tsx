import { DashboardClient } from './components/dashboard-client';

export const metadata = {
  title: 'Dashboard de Vendas | naraka-bi',
};

export default function DashboardPage() {
  return <DashboardClient />;
}
